Base de Datos e Inicio: 


- En caso de no tener instaladas las dependencias correr el siguiente npm:

 npm i axios bcrypt cookie-parser express jsonwebtoken morgan nodemon pg pg-hstore sequelize

- Hacer un createdb con el nombre de la base de datos detallada ("al_lio")

- Iniciar con "npm start"